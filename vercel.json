{
  "buildCommand": "test -d frontend && cd frontend || true; npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "test -d frontend && cd frontend || true; npm install",
  "framework": "vite"
}
